<Page x:Class="Test_Management_System.Pages.PageCheckListItems"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Test_Management_System.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1150" 
      Title="PageCheckListItems">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Заголовок страницы -->
        <Label Content="Список пунктов чек-листов" FontSize="16" FontWeight="DemiBold" Margin="10" />

        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Margin="0 0 0 5" Content="Описание:"/>
            <TextBox Grid.Row="0" x:Name="TextBoxDescription" Width="1030"  HorizontalAlignment="Right" Margin="5, 5, 10, 5"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0, 10, 0, 10">
                <Label  Margin="0, 0, 113, 0" Content="Статус:"/>
                <ComboBox x:Name="ComboBoxStatus" Width="400" Padding="10" DisplayMemberPath="CLStatusDescriptionTranslation"/>
                <Label  Margin="75, 0, 72, 0"  Content="Приоритет:"/>
                <ComboBox x:Name="ComboBoxPriority" Width="400" Padding="10" DisplayMemberPath="CLPriorityDescriptionTranslation"/>
            </StackPanel>

            <Label Grid.Row="2" Margin="0, 5, 0, 5" Content="Комментарий:"/>
            <TextBox Grid.Row="2" x:Name="TextBoxComment" Width="1030" HorizontalAlignment="Right" Margin="5, 5, 10, 5"/>

            <Label Grid.Row="3"  Content="Вложение:" Margin="0 0 0 5"/>
            <ListBox Grid.Row="3" Width="975" Height="70" HorizontalAlignment="Right" Margin="5,5,65,5"  x:Name="AttachmentsListBox">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}" Margin="10" FontSize="14" FontFamily="Courier" />
                            <Button Content="Удалить" Click="RemoveAttachment_Click"  Margin="10, 0" Width="100" Style="{DynamicResource RoundedCorners}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Style="{DynamicResource RoundedCorners}" Grid.Row="3" Content=" + " FontSize="26" Padding="5" Click="AddAttachmentToCheckList_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5, 5, 10, 5"/>

            <Button Grid.Row="4"   Content="Добавить пункт" Name="AddChecklistItem" Click="AddChecklistItem_Click" Margin="10" Style="{DynamicResource RoundedCorners}" />
            <DataGrid Name="CLListView"  Grid.Row="5" Margin="10" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Описание" Width="200" Binding="{Binding CheckListItemDescription}"/>
                    <DataGridTextColumn Header="Статус" Width="100" Binding="{Binding CheckListStatus.CLStatusDescriptionTranslation}"/>
                    <DataGridTextColumn Header="Приоритет" Width="100" Binding="{Binding CheckListPriority.CLPriorityDescriptionTranslation}"/>
                    <DataGridTextColumn Header="Комментарий" Width="300" Binding="{Binding CLComment}"/>
                    <DataGridTextColumn Header="Вложение" Width="150" Binding="{Binding CLAttachment}"/>
                    <DataGridTemplateColumn Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{DynamicResource RoundedCorners}" Content=" X " Click="DeleteCheckListItem" Padding="5" Tag="{Binding}" FontSize="20" Margin="5"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <!--<Button Template="{StaticResource beautifulButton}"  Width="250" Padding="10" Margin="10" Content="Создать и выйти" Name="AddItemToCheckListAndExit" Click="AddItemToCheckListAndExit_Click"/>
            <Button Template="{StaticResource beautifulButton}" Width="250" Padding="10" Margin="10" Content="Создать и перейти к следующему" Name="AddItemToCheckListGoToAnother" Click="AddItemToCheckListGoToAnother_Click" />-->
            <Button Style="{DynamicResource RoundedCorners}" Width="200" Margin="10, 10, 10, 20" Content="Выйти к чек-листам" Name="ExitFromAddingCheckListItem" Click="ExitFromAddingCheckListItem_Click"/>
        </StackPanel>
    </Grid>
</Page>
